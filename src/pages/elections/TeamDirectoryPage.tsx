import React from "react";
import TeamDirectory from "../../components/elections/TeamDirectory";

const TeamDirectoryPage: React.FC = () => {
  return <TeamDirectory />;
};

export default TeamDirectoryPage;
